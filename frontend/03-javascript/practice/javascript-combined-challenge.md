# Practice: Combined JavaScript Challenge

## Tasks

### Task 1
Build a countdown timer: input for seconds, Start/Stop/Reset buttons. Display MM:SS. Use setInterval. Pause and resume. Reset to initial value. Clear interval on stop. Prevent negative or invalid input.

### Task 2
Create a simple image carousel: 3–5 images, prev/next buttons, auto-advance every 3 seconds. Loop (after last, go to first). Pause auto on hover. Show dots for current slide. Use a state variable for current index.

### Task 3
Build a searchable dropdown: input that filters a list of options. Click to select. Show selected value. Keyboard: arrow keys to navigate, Enter to select. Use a list of items (e.g., countries). Filter as user types. Debounce optional.

### Task 4
Implement a simple state machine: a traffic light (Red -> Green -> Yellow -> Red). Or a login form (idle -> loading -> success/error). Use state variable and transitions. Render UI based on state. Add buttons or timers to trigger transitions.

### Task 5
Build a mini dashboard: fetch data from 2 different APIs (e.g., users and posts from JSONPlaceholder). Display in 2 sections. Add loading skeletons. Add error boundaries (if one fails, show others). Add a refresh button. Cache the data (don't refetch on every refresh). Show last updated time.
